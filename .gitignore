# Binaries
*.exe
*.dll
*.so
*.dylib
setup/setup

# Test + coverage
*.test
*.out
coverage.*

# Editor / OS
.DS_Store
.idea/
.vscode/

# MkDocs build output
site/

# Vendored deps (modules are fetched via the proxy; never commit vendor/)
/vendor/

# Local-only working notes / parked TODOs (never committed)
/.local/
